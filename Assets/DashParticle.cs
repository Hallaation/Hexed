using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DashParticle : MonoBehaviour {

    BaseAbility Charge;
	// Use this for initialization
	void Start () {
       
	}
	
	// Update is called once per frame
	void Update () {
        //Charge = transform.root.GetComponent<BaseAbility>();
        //Timer CoolDownTime = transform.root.GetComponent<BaseAbility>().GetCoolDownTimer();
        
        //var main = GetComponent<ParticleSystem>().main;
        //main.startSize = 0.5f * Charge.ReturnCurrentCharges() + CoolDownTime.CurrentTime / 4; 
       
    }



}
